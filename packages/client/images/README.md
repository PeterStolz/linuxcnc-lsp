# Marketplace images

Referenced by `../README.md` (the marketplace listing) via absolute
`raw.githubusercontent.com/.../main/packages/client/images/...` URLs, so they
render on both GitHub and the VS Code Marketplace once on `main`.

Captured from the demo config in `docs/demo/`:

| File | Shot |
|------|------|
| `hero-diagnostics.png` | live error-checking on a HAL file: the `[JOINT_0]IGAIN` missing-INI-key squiggle plus hover |
| `hover-docs.png` | hover documentation from the LinuxCNC source (an INI `[TRAJ]COORDINATES` key) |
| `gcode-subroutines.png` | a G-code O-word subroutine (`o<probe>` sub/if/endif) with a `G38.2` hover doc |

PNG, roughly 1200px wide, cropped tight to the relevant editor region.
